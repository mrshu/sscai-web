<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta n:ifset="$robots" name="robots" content="{$robots}">

	<title> {block title|striptags|trim}{/block} | SSCAI Tournament </title>

	<link rel="stylesheet" media="screen" href="{$basePath}/css/site.css" />
</head>

<body>
	<div n:foreach="$flashes as $flash" class="flash {$flash->type}">{$flash->message}</div>

	<div id="content">
		{include content}
	</div>

	<p id="logged-in" n:if="$user->loggedIn">Signed in as {$user->identity->full_name}. <a n:href="Sign:out">Sign out</a></p>

	<script src="http://nette.github.com/resources/js/netteForms.js"></script>
</body>
</html>
